# Cloud photo delivery (Cloudflare Worker + R2). Copy to `.env` and fill in.
# Without this, the QR falls back to a non-working placeholder link.
# See README → "Photo delivery (cloud, 24h expiry)" and the cloudflare/ folder.

VITE_DELIVERY_BASE=https://madshotz-delivery.YOURNAME.workers.dev
